
<%= form_tag destroy_multiple_partituras_path, method: :delete, data: {confirm: "You are sure this item?"} do %>
<table class="table">
<tr>
 <%= submit_tag 'Deletar Selecionado', class: 'button is-danger is-small' %> 
 <th data-sort-ignore="true"><input type="checkbox" id="selectAll"></input></th>
<th>Title</th>
<th>Description</th>
<th>link</th>
<th>Action</th>
</th>
<tbody>
<% @partitura.each do |sheet| %>
<tr>
<td><%= check_box_tag "partituras_ids[]", sheet.id, false, class: 'selectable' %></td>
<td><%= sheet.title %></td>
<td><%= truncate(sheet.description, length: 30 ) %></td>
<td><%= link_to 'visualizar', "#{sheet.link}", class: 'button is-outlined is-link', target: '_blank' %></td>
<td class="buttons">
<%= link_to "Publicar", edit_partitura_path(sheet), class: 'button is-success' %>
<%= link_to "Deletar", partitura_path(sheet), method: :delete, data: {confirm: "Você realmente deseja deletar esse item
                 #{sheet.id}?"}, class: "button is-outlined is-danger is-small" %></td>
</tr>
<% end %>
</tbody>
</table>

<% end %>